# Per-task NeuroGolf-2026 golf agent — instructions

You golf ONE task (the task number is given to you). Goal: an ONNX graph that EXACTLY
reproduces every (input,output) pair AND costs LESS (memory+params) than the current base
graph, implementing the TRUE general rule (must survive a hidden private test set — do NOT
overfit/memorize). If you beat base, save `out/search/taskNNN.onnx`. If you cannot, skip it.

Workdir: `/home/woody/dsaa/dsaa115h/Kaggle-Competitions/Better_Golf`.
Python: `/home/woody/dsaa/dsaa115h/software/private/conda/envs/kaggle/bin/python` (onnx 1.21,
ort 1.24.4, numpy, torch). DO NOT submit to Kaggle. Grade in a scratch CWD (e.g. $TMPDIR) so ORT
profiler json files don't litter the repo. Use compute freely; only verify()-ok graphs count.

## Data + GROUND-TRUTH grader (reuse; never reimplement)
- `from engine import dataio; t=dataio.load_task(n)` -> {"train","test","arc-gen"} lists of
  {"input":grid,"output":grid}. ~268 pairs; the 262 arc-gen are generated by the TRUE rule
  (your generalization signal — study several to infer the rule). `dataio.to_onehot(grid)->[1,10,30,30] f32`.
- `from engine.verify import verify; r=verify(model,t,n)` -> {ok,n_pass,n_fail,memory,params,points,
  disqualified,err}. ok==True iff exact on ALL pairs AND measurable. Accept nothing else.

## Cost model (minimize memory+params)
score=max(1,25-ln(memory+params)). memory=Σ intermediate-tensor bytes (elements*dtype_bytes,
max(static,ORT-runtime)); **`input` and `output` tensors are FREE**. dtype bytes: bool/int8/uint8=1,
float16=2, float32=4, int64=8. params=Σ initializer+Constant element counts. **Node ATTRIBUTES are
NOT counted** — Transpose `perm`, and in OLD opsets Slice/Pad/Reshape/Squeeze params are attributes
= FREE. ALL tensor shapes must be static (any dynamic dim => disqualified=0). Banned ops:
Loop/Scan/NonZero/Unique/Compress/Sequence*. opset domain ""/"ai.onnx" (any version). File <=1.44MB.
Decode=(output>0.0) vs one-hot: each cell exactly the true color channel >0, others <=0 (color 0 is a
channel; out-of-grid cells = all channels 0).

## Inspect the base graph you must beat
```
import zipfile,onnx
m=onnx.load_model_from_string(zipfile.ZipFile('out/submission.best-6373.zip').read(f'task{n:03d}.onnx'))
# verify(m,t,n) -> its cost. Base graphs are sophisticated (95-649 nodes, already fp16+single-channel).
```
To beat base you need a SHORTER program (fewer/smaller intermediates) for the SAME function.

## Golf levers (use these)
1. INDEX SPACE: ArgMax(input,axis=1)->[1,1,30,30] cast uint8 (900B) collapses 10 one-hot channels to 1.
   Transform on this single channel, re-expand via Equal vs a [10] color const -> `output` (free).
   ~10-40x cheaper than [1,10,30,30] float intermediates.
2. Emit the final result DIRECTLY into `output` (free) — no trailing Cast/Identity.
3. Free attribute-ops for geometry/movement (Transpose perm; old-opset Slice/Pad/Reshape). NOTE: grids
   are anchored TOP-LEFT in a fixed 30x30 frame, variable content extent — full-frame flip is WRONG
   (extent problem); transpose keeps the top-left square in place. Handle extent.
4. Narrow EVERY intermediate to bool/uint8 where values allow; re-verify the (>0) decode holds.
5. Infer the EXACT rule from arc-gen, build the MINIMAL ops implementing exactly it, verify n_fail==0
   on ALL pairs. Exact on all 262 arc-gen ≈ true rule ≈ private-safe.

## Method
analyze rule -> design minimal program -> build static-shape ONNX -> verify() -> if ok AND cost<base:
save out/search/taskNNN.onnx; else iterate (try index-space / different ops) or skip.

## Report (final message, concise, REAL verify numbers only)
task | base_cost -> your_cost (or "no win") | base_pts->your_pts | the rule | technique used.
