# (decompressed)
def p(r,u=10):
	a=range(u*u);f,n=[{(q//u,q%u)for q in a if r[q//u][q%u]==f}for f in(2,0)];p,c=map(min,zip(*f));i=[q for q in a if(i:={(q//u+j-p,q%u+w-c)for j,w in f})<=n and(n:=n-i)]
	if(i[0]==17)*i[3:]:i[1]=60
	for a in i[i==[13,56]:]:
		for j,w in f:r[a//u+j-p][a%u+w-c]=2
	return r
